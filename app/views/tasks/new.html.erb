<h1 class="title" >New Task ✏️</h1>
<%= form_with model: @task do |t| %>
  <%= t.label :title, class:  %>
  <%= t.text_field :title %>
  <%= t.label :details %>
  <%= t.text_field :details %>
  <%= t.submit "Create Task "%>
<% end %>
<%= link_to "Back to list ", tasks_path %>
